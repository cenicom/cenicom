Siguiente incremento exacto

Yo continuaría con:

10.9.4.1 — View → Laravel Namespace Bridge

Objetivo:

Cada ViewDefinition registrada por un módulo debe terminar registrada como namespace real de Laravel, manteniendo además el registro neutral de CN.

Y lo implementaría en este orden:

No modificar contratos existentes.
Adaptar ViewRegistrar para recibir Illuminate\Contracts\View\Factory.
Registrar primero en ViewRegistry.
Registrar el namespace en Laravel.
Preferiblemente usar replaceNamespace() para mantener semántica último registro gana.
Actualizar bindings si el container ya resuelve Factory correctamente — probablemente no será necesario.
Crear test unitario del bridge.

Crear test funcional de resolución:

view('institution::...')

Crear/corregir fixture real:

tests/Fixtures/View/views/
Ejecutar exclusivamente el bloque View.
Después ejecutar toda la suite.

No tocaría CN-GUI, CRUD ni Navigation. Este incremento debe cerrar exclusivamente la frontera:

Module → ViewDefinition → CN View Registry → Laravel View Namespace

Además, el hecho de que tengas ya 101 tests / 174 assertions pasando nos da una base limpia para hacer este cambio de forma incremental.
/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*
Orden que recomiendo

10.10 — Institution Model

Revisar la migración institutions.
Revisar el model.stub actual.
Revisar el patrón de modelos generado.
Crear app/Models/Institution.php.
Crear InstitutionTest.php.
Ejecutar el test específico.
Ejecutar el conjunto de tests del Generator + Core + Institution.

No tocaría todavía Controller, Repository, Service ni vistas. Primero dejamos Model → Migration perfectamente alineados.
****************************************************************************
El cambio inmediato que haría es actionPath en directories(), porque ese sí es un defecto funcional, no solamente una cuestión de estilo. Después extraería SeederBuilder y hacemos una auditoría sistemática de ModuleData para comprobar que no exista otro desalineamiento oculto.
