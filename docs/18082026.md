10.x — Consolidación del CN Generator Provider

Estado previo: implementación y cobertura → ✅ cerradas
Suite: 127 passed (445 assertions) → ✅
Siguiente objetivo: consolidar CNGeneratorServiceProvider → 🟡

La secuencia que recomiendo es:

Auditar CNGeneratorServiceProvider
StubManager
GeneratorValidator
GeneratorTestSuite
GeneratorRegistry
ModuleGenerator
Crear la prueba específica del Provider, si no existe.
Mover al Provider activo únicamente lo que realmente corresponda.
Ejecutar la prueba específica.

Ejecutar nuevamente:

php artisan test tests/Unit/Core/Generator
Verificar que GeneratorServiceProvider.php ya no tenga ninguna responsabilidad exclusiva.
Eliminar el Provider legado.
Limpiar referencias/cache y ejecutar nuevamente la suite.
Arquitectura objetivo
CNGeneratorServiceProvider
│
├── infraestructura común
│   ├── StubManager
│   ├── GeneratorValidator
│   └── GeneratorTestSuite
│
├── GeneratorRegistry
│   └── todos los GeneratorInterface
│
└── ModuleGenerator
    ├── Registry::all()
    └── GeneratorTestSuite

No tocaría ahora ningún Generator, Builder ni GeneratorTestCase. Ya están certificados.

El siguiente paso concreto es auditar las responsabilidades de CNGeneratorServiceProvider y escribir su prueba antes de elimina
**********************************************************************
✅ CN-GEN ServiceProvider
   ↓
✅ GeneratorRegistry
   ↓
✅ ActionGenerator
   ↓
✅ ModuleManifestGenerator
   ↓
✅ 1069 tests / 2434 assertions
   ↓
🔵 10.9.1 Module → Navigation
   ↓
   ├── RegisterNavigationStep
   ├── ModuleData::navigation()
   ├── ModuleDataFactory::buildNavigation()
   ├── Navigation Manifest
   ├── Navigation Discovery
   └── Navigation Registry
   ↓
🔵 certificación Module → Navigation
   ↓
🟢 10.9.2 Module → Permissions
   ↓
🟢 10.9.3 Module → CRUD
   ↓
🟢 10.9.4 Module → Views
   ↓
🟢 10.9.5 Security → GUI
******************************************************************************
🫡 Rumbo trazado.

🚦 Misión activa

10.9.5 — Security → GUI

Objetivo de la maniobra: integrar y certificar el consumo de CN-SEC desde la capa GUI, sin modificar innecesariamente los contratos de seguridad ya certificados.

Orden de operaciones
Auditar el punto de contacto Security ↔ GUI
permisos;
identidad;
resolvers;
componentes/directivas GUI existentes;
integración con navegación y acciones.
Determinar el contrato real
qué debe consultar la GUI;
qué interfaz debe consumir;
dónde debe ocurrir la decisión de autorización;
qué comportamiento debe tener cuando el permiso existe/no existe.
Escribir primero las pruebas de contrato
caso autorizado;
caso denegado;
ausencia de permiso;
integración con elementos GUI relevantes.
Implementar únicamente lo necesario.
Certificar en verde.
Checkpoint final de 10.9.
Regla de puente

No tocar CN-GUI congelado salvo que la certificación demuestre una necesidad real.
No duplicar lógica de CN-SEC dentro de GUI.
GUI consume contratos; Security decide.

La maniobra anterior queda cerrada:

10.9.4 Module → Views       🟢 9 tests / 73 assertions
                           │
                           ▼
10.9.5 Security → GUI      🟡 MISIÓN ACTIVA

Listos para auditar el punto exacto de integración Security → GUI.
*******************************************************************
10.9.3 Module → CRUD

10.9.3.1 CrudDefinition
   ├── CrudDefinitionInterface
   ├── ModuleDefinition.crudDefinitions
   ├── ModuleDefinitionFactory
   └── tests
              ↓
10.9.3.2  CrudRegistrarInterface + CrudRegistrar
              ↓
10.9.3.3  CrudRegistry
              ↓
10.9.3.4  CrudDefinitionRegistry/Loader
              ↓
10.9.3.5  CrudService
              ↓
10.9.3.6  Module → CRUD
              ↓
10.9.3.7  ResourceService
             ↓
10.9.3.8  Module → CRUD registration
             ↓
10.9.3.9  ResourceService
             ↓
10.9.3.10 Container bindings
             ↓
10.9.3.11 Tests de integración

*************************************************************************
La siguiente maniobra concreta será:

10.9.3.2 — CrudRegistrarInterface → prueba de contrato

y después:

10.9.3.3 — CrudRegistrar → implementación + prueba

Así mantenemos el puente limpio y no inventamos todavía CrudRegistry ni ResourceService.
