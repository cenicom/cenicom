Pero mantendría todavía una última comprobación antes de corregir: ver las pruebas completas de ViewLaravelNamespaceBridgeTest, ViewModuleResolutionTest y ViewModuleIsolationTest, porque ahí podremos demostrar si ese uso de :: es una expectativa contractual del Core y no simplemente una implementación interna de las pruebas.

No cambiamos nada todavía. La investigación está correctamente acotada a la frontera InstitutionController ↔ ViewDefinition/ViewRegistrar.
/**/*-/*-/*-/*-/*-/*/*-/*-/-*/-*/*-/-*/-*/*-/-*/-*/-*/-*/-*/-*/-*/-*/-*/-*/-*/-*/-*/-*/*-/-*/*-/*-/*-
Conclusión

La incompatibilidad no está en ViewRegistry, ViewRegistrar, ViewBootstrapper ni ModuleData.

Está en los 6 puntos de los stubs del generador.

Siguiente maniobra

Antes de cambiar esos seis puntos, debemos proteger el contrato con los tests que actualmente esperan la sintaxis antigua.

Ejecuta:

grep -R -n -E "return view\(|@include\(" \
tests/Unit/Core/Generator/Generators \
tests/Unit/Core/Generator/Builders \
tests/Unit/Core/Generator/Factories

Así identificamos exactamente qué assertions tendrán que evolucionar junto con el contrato.

⚓ Ya estamos en “contrato → impacto → corrección”; falta cerrar la evidencia de tests antes de editar.
