
*************************************************************************
Propongo avanzar en este orden:

Identificar el punto de entrada
ModuleGenerator
GeneratorManager
pipeline/orquestación
cómo llega ModuleData al bloque CRUD.
Auditar los artefactos CRUD generados
Model
Repository / RepositoryInterface
Service / ServiceInterface
Actions
Requests
Controller
rutas, si forman parte del contrato CRUD actual.
Verificar contratos
namespaces generados vs. app/Core/Crud
interfaces implementadas
dependencias inyectadas
nombres de clases
ubicación de archivos.
Verificar integración modular
que el Generator genere CRUD dentro del módulo correcto;
que no dependa de rutas rígidas de app/;
que respete ModuleData;
que un módulo arbitrario funcione igual que Institution.
Tests
primero tests unitarios existentes;
después un test de integración mínimo Generator → CRUD;
finalmente suite completa.
Criterio de cierre

No consideraría terminado 10.9.3 simplemente porque cada generator individual pase sus tests.

Debe quedar demostrado:

ModuleData → Generator → CRUD artifacts → contratos Core/Crud coherentes

sin introducir ninguna dependencia de CN-GUI.

Así mantenemos limpia la frontera:

10.9.3 = Module → CRUD
10.9.4 = Module → Views
10.9.5 = Security → GUI

Y podemos continuar directamente con la auditoría del código actual, empezando por el punto donde ModuleGenerator decide qué generators CRUD ejecutar.
