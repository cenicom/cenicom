Orden de trabajo que recomiendo

No cambiaría todo de una vez. Haría esta secuencia:

1. PermissionMatrixFactory
   └── eliminar dependencia de ModuleData

2. PermissionMatrixFactoryTest
   └── adaptar fixtures

3. ModuleData
   └── PermissionMatrix obligatorio

4. ModuleDataFactory
   └── construir PermissionMatrix antes de new ModuleData

5. MiddlewareBuilder
   └── eliminar null-check de matriz

6. PermissionBuilder
   └── eliminar 3 null-checks de matriz

7. RegisterPermissionsStep
   └── ya está prácticamente correcto

8. búsqueda global
   └── verificar que no quede permissionMatrix nullable/null
************************************************************************
Siguiente paso concreto

Yo haría una sola modificación primero, sin tocar Controller ni Institution:

Revisar resources/stubs/service-interface.stub.
Confirmar cómo queda create() en XxxServiceInterface.
Corregir resources/stubs/action.stub para que update() use getKey().
Fortalecer ActionGeneratorTest para capturar exactamente ese contrato.
Regenerar GeneratorProbeAction/fixture si corresponde.

Ejecutar únicamente:

php artisan test \
    tests/Unit/Core/Generator/Support/Action/ActionBuilderTest.php \
    tests/Unit/Core/Generator/Generators/ActionGeneratorTest.php
Si queda verde, entonces ejecutar la suite completa.

Antes de editar nada, pásame únicamente resources/stubs/service-interface.stub y, si existe, el GeneratorProbeServiceInterface generado. Con eso cerramos definitivamente la duda de create() y hacemos la corrección de Action con el contrato completo.
