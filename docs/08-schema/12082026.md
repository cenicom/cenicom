Mi propuesta concreta para cerrar CN-AUD-004

En este orden:

Crear AuditEntryData.
Cambiar AuditQueryInterface para que devuelva AuditEntryData.
Modificar EloquentAuditQuery para mapear AuditLog → AuditEntryData.
Revisar el binding del contrato en el provider.
Crear AuditQueryResolutionContractTest.
Probar:
resolución del contrato;
implementación concreta;
lectura desde audit_entries;
mapeo correcto;
null cuando no existe;
nunca devuelve AuditLog.
Ejecutar primero el test específico.
Después ejecutar la batería CN-AUD completa.
