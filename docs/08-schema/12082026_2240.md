🧭 Paso 10.5.4

Ahora sí corresponde introducir el PolicyResolver, pero de forma incremental:

PolicyRegistryInterface
        │
        ▼
   PolicyResolver
        │
        ▼
   PolicyInterface

Yo lo dividiría así:

10.5.4.1 — PolicyResolverInterface + test de contrato
10.5.4.2 — implementación PolicyResolver + tests
10.5.4.3 — binding en SecurityServiceProvider + test de container
10.5.4.4 — integración real Registry → Resolver → Policy
/*/*/*/*/*/*/*/*/*/*/*/*/**/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/*/
Propongo continuar así

10.5.5 — SpecificationPolicy

Primero:

10.5.5.1 — SpecificationPolicyInterface
           + test de contrato

10.5.5.2 — SpecificationPolicy
           + tests

10.5.5.3 — integración Specification → Policy

10.5.5.4 — integración Policy → Registry → Resolver

Después:

10.5.6 — ResourceSpecificationPolicy

10.7  Security → Navigation → GUI
      ├── 10.7.1 Authorization → Navigation
      ├── 10.7.2 NavigationPermissionResolver
      ├── 10.7.3 integración Security/NAV
      ├── 10.7.4 primer flujo GUI
      └── 10.7.5 Sidebar autorizado

10.8  CN-GUI / Design System
      ├── layout
      ├── topbar
      ├── sidebar
      ├── alerts
      ├── forms
      ├── tables
      └── CRUD UI

10.9  integración GUI ↔ módulos
