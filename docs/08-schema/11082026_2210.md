🚦 Siguiente maniobra: cerrar CN-AUD-004

Antes de crear otra capa, haría una prueba de contrato de resolución completa:

AuditQueryInterface
        │
        ▼
EloquentAuditQuery
        │
        ▼
audit_entries

Pero hay algo más importante: ahora que tenemos escritura + lectura, debemos evitar que EloquentAuditQuery devuelva directamente AuditLog hacia las capas superiores.
