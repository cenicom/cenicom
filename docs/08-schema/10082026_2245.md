Estado del bloque CN-NAV

NavigationSecurityPersistenceTest ya cubría la persistencia de autorización.

Ahora:

NavigationAuthorizationCacheConsistencyTest → 7/7 PASS

Por tanto, el siguiente paso natural es no seguir agregando casos arbitrariamente, sino ejecutar la batería completa de Navigation para detectar regresiones entre:

Navigation Registry
Builder
Service
Cache
Authorization
Invalidation
Rendering

Ahí podremos declarar este tramo de CN-NAV + CN-SEC estabilizado antes de avanzar al siguiente hito.
