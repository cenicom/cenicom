Próximo movimiento de cubierta recomendado

🚢 CN-GEN-ORCH-002.1.2 — Completar reglas de ValidateModuleStep

Antes de conectar ExecuteGeneratorsStep, conviene que esta etapa tenga validaciones reales sobre ModuleData:

Propuesta:

ValidateModuleStep
        |
        ├── validateIdentity()
        ├── validateNamespaces()
        ├── validateClasses()
        ├── validatePaths()
        └── validateFields()

y luego certificar:

php artisan test --filter=ValidateModuleStepTest

Después de esa cubierta:

🚢 CN-GEN-ORCH-002.2 — ExecuteGeneratorsStep → conexión con GeneratorManager.
