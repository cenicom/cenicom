🚢 SIGUIENTE MANIOBRA

Ahora sí estamos en el punto que habíamos identificado:

CN-MOD-BOOT-LIFECYCLE-002 — Lifecycle ↔ Pipeline

Actualmente ModuleBootstrap hace esto:

Pipeline
   │
   └── procesa completamente el módulo
            │
            ▼
       resultado final
            │
            ▼
Lifecycle:
DISCOVERED
REGISTERED
BOOTING
BOOTED
RUNNING

Es decir, el Lifecycle se actualiza después del pipeline.

La siguiente maniobra será comprobar si podemos hacer que represente realmente la ejecución:

DISCOVERED
    │
    ▼
REGISTERED
    │
    ▼
BOOTING
    │
    ▼
Pipeline
 ├── éxito ──────► BOOTED ──► RUNNING
 │
 ├── skipped ────► SKIPPED
 │
 └── failure ────► FAILED
🎯 Pero primero una prueba de seguridad

Antes de mo
