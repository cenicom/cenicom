Siguiente candidato natural

Ahora conviene continuar con RequestGenerator, porque en la secuencia anterior ya identificamos que existe infraestructura de construcción de Requests y además fue precisamente uno de los puntos donde aparecieron fallos en RequestBuilder.

Primero hagamos inventario:

find app/Core/Generator -type f \
  \( -name 'RequestBuilder.php' -o -name 'RequestGenerator.php' \) \
  -print


find tests/Unit/Core/Generator -type f \
  -iname '*Request*' \
  -print
