Paso 1 — localizar constructores de ModuleData

Ejecutaría:

grep -R -n \
"new ModuleData(" \
app tests \
--exclude-dir=vendor \
--exclude-dir=storage
Paso 2 — localizar cualquier comprobación de null
grep -R -nE \
"permissionMatrix\(\).*null|permissionMatrix\(\)|permissionMatrix.*null|=== null|!== null" \
app/Core/Generator \
tests/Unit/Core/Generator \
--exclude-dir=vendor \
--exclude-dir=storage
Paso 3 — revisar el comienzo completo de ModuleDataFactory

Porque necesitamos ver exactamente cómo create() arma el ModuleData y dónde debe entrar PermissionMatrixFactory.

Contrato que ya podemos considera
