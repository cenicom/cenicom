Siguiente paso

Entonces sí pasamos a:

10.10.2-C.1 — Migrar los consumidores GUI heredados al boundary Adapter.

El primer candidato es:

tests/Feature/Views/Components/Crud/CrudSecurityGuiRegressionTest.php

Ahí debemos cambiar el flujo de los tests de:

Filter → CrudActionView → Presenter → GUI

a:

Filter → Presenter → Presentation → Adapter → CrudActionView → GUI

sin tocar nuevamente CrudActionPresenter, CrudActionPresentation ni CrudAction.
