El siguiente punto lógico es verificar City HTTP CRUD, empezando por GET /cities y luego create/store/edit/update/destroy, porque las pruebas que acabamos de hacer todavía no atraviesan Controller + FormRequest + rutas.
/-/-*/-*/*-/*-/*-/*/*-/*-/*-/-*/*-/*-/*-/-*/*-/*-/*-/*-/*-/*/*-/-*/-*/-*/-*/-*/*-/*-/-*/-*/-*/-*/*-/-*/-*
FASE 1 — CONTRATO
────────────────────────
ModuleData
    ↓
paths/classes necesarios

FASE 2 — TESTS
────────────────────────
ModuleManifestGeneratorTest
ViewDefinitionGeneratorTest
ModuleDataFactoryTest
PrepareDirectoriesStepTest

FASE 3 — STUBS
────────────────────────
module.stub
view-definition.stub

FASE 4 — GENERATORS
────────────────────────
ModuleManifestGenerator
ViewDefinitionGenerator

FASE 5 — INTEGRACIÓN
────────────────────────
ModuleDataFactory
PrepareDirectoriesStep
CNGeneratorServiceProvider

FASE 6 — EVIDENCIA REAL
────────────────────────
cn:make-module City --force
        ↓
module.php
CityView.php
        ↓
ModuleBootstrap
        ↓
ViewBootstrapper
        ↓
cities namespace
        ↓
GET /cities
GET /cities/create
